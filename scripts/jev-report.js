function renderReport(report) {
  const data=JSON.stringify(report).replace(/</g,'\\u003c').replace(/\u2028/g,'\\u2028').replace(/\u2029/g,'\\u2029');
  return `<!doctype html>
<html lang="ja"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>プロジェクトの記憶 — Jev試作</title>
<style>
:root{color-scheme:light;--ink:#172b35;--muted:#536770;--line:#d9e2e1;--accent:#086d64}*{box-sizing:border-box}body{margin:0;background:#f3f6f5;color:var(--ink);font:15px/1.75 system-ui,-apple-system,sans-serif}header,main{max-width:1160px;margin:auto;padding:30px}header{padding-bottom:8px}.eyebrow{color:var(--accent);font-size:12px;font-weight:700;letter-spacing:.14em}h1{font-size:clamp(26px,4vw,40px);margin:5px 0}h2{font-size:19px;margin:0 0 12px}p{margin:8px 0}.muted{color:var(--muted)}.notice{border-left:4px solid #c68b2c;background:#fff6e4;padding:13px 18px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:22px}.stat{background:white;border:1px solid var(--line);border-radius:12px;padding:15px}.stat b{display:block;font-size:24px}.stat span{color:var(--muted);font-size:12px}.layout{display:grid;grid-template-columns:240px 1fr;gap:26px}aside{align-self:start;position:sticky;top:20px}button,select,input{font:inherit}button{cursor:pointer}button.filter{width:100%;text-align:left;border:1px solid var(--line);background:#fff;padding:9px 12px;border-radius:9px;margin:3px 0;line-height:1.5;color:var(--ink)}button.filter.active{background:var(--accent);color:white;border-color:var(--accent)}.group{margin:20px 0}.controls{display:flex;gap:12px;flex-wrap:wrap;align-items:center;margin-bottom:15px}.controls input{min-width:180px;flex:1}.controls input,.controls select{border:1px solid var(--line);padding:8px;border-radius:8px;background:#fff;max-width:100%}.timeline{border-left:2px solid #bdd2cd;margin-left:8px;padding-left:25px}.card{position:relative;margin:0 0 20px;padding:20px;background:white;border:1px solid var(--line);border-radius:12px;overflow-wrap:anywhere}.card:before{content:'';position:absolute;left:-33px;top:27px;width:12px;height:12px;background:var(--accent);border:3px solid #f3f6f5;border-radius:50%}.card h3{font-size:18px;line-height:1.5;margin:6px 0}.card a{color:#086d64;text-decoration:none}.card a:hover{text-decoration:underline}.chips{display:flex;gap:6px;flex-wrap:wrap;margin:8px 0}.chip{border-radius:6px;padding:2px 8px;font-size:12px;background:#e8f3ef;color:#185d50}.chip.problem{background:#fff0df;color:#8b520d}.chip.plan{background:#eaf0ff;color:#365596}.chip.completed{background:#e3f3e4;color:#306837}.chip.review{background:#f0edf6;color:#675782}details{margin-top:12px}summary{cursor:pointer;color:#35585f}.evidence{border-top:1px solid #e8eeec;padding:12px 0}.quote{white-space:pre-wrap;font-size:14px;margin:8px 0;padding-left:12px;border-left:3px solid #d4e2dd}.tiny{font-size:12px;color:var(--muted)}.panel{background:#fff;border:1px solid var(--line);padding:20px;border-radius:12px;margin:20px 0}table{width:100%;border-collapse:collapse;font-size:13px}td,th{text-align:left;vertical-align:top;border-bottom:1px solid var(--line);padding:9px 6px}footer{padding:20px 0;color:var(--muted);font-size:12px}.empty{padding:30px;background:white;border-radius:12px}@media(max-width:720px){header,main{padding:18px}.stats{grid-template-columns:repeat(2,1fr)}.layout{display:block}aside{position:static}.group{margin:12px 0}.filterlist{display:flex;overflow:auto;gap:6px;padding-bottom:5px}button.filter{flex:0 0 auto;width:auto}.timeline{padding-left:19px}.card:before{left:-27px}.card{padding:15px}table{font-size:12px}.controls{gap:8px}}
</style>
<header><div class="eyebrow">VA-CH / PROJECT MEMORY / PILOT</div><h1>プロジェクトの記憶</h1><p class="muted">取り組みをたどり、次に確かめたいことを見つける。</p><div class="notice" id="notice"></div><div class="stats" id="stats"></div></header>
<main><div class="layout"><aside><button type="button" class="filter active" data-kind="all" data-key="all">すべての動画</button><div class="group"><h2>トピック</h2><div id="topics" class="filterlist"></div></div><div class="group"><h2>プロジェクト候補</h2><div id="projects" class="filterlist"></div></div><p class="tiny">プロジェクト名は試作用に定義した候補です。関連判定0.5以上を候補に含め、0.8未満は要確認として表示します。イベントとの対応付けは未確定です。</p></aside><section><div class="controls"><input id="search" type="search" aria-label="原文・タイトルを検索" placeholder="原文・タイトルを検索"><select id="event" aria-label="記述の種類"><option value="all">すべての記述</option><option value="plan">計画・希望あり</option><option value="performed">実施・進展あり</option><option value="completed">完成・解決の記述あり</option><option value="problem">問題・保留の記述あり</option><option value="review">要確認の判定あり</option></select><select id="order" aria-label="並び順"><option value="asc">古い順</option><option value="desc">新しい順</option></select></div><p id="count" class="muted" aria-live="polite"></p><p class="tiny">日付は動画公開日です。概要欄の追記日・実際の作業日は未確認。「問題あり」は過去の問題も含み、現在の未完了を意味しません。</p><div id="timeline" class="timeline"></div></section></div>
<section class="panel"><h2>今回の実測と確認</h2><div id="metrics"></div><details><summary>原文から事前に設定した確認項目</summary><p class="tiny">意図的に選んだ少数の確認項目です。独立した精度評価ではなく、字幕全体への性能は未測定です。yes ≥ 0.8、no ≤ 0.2、その間は要確認という仮の基準です。</p><div id="checks"></div></details></section><footer id="footer"></footer></main>
<script id="report-data" type="application/json">${data}</script>
<script>
const D=JSON.parse(document.getElementById('report-data').textContent);
const el=(tag,text,cls)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n;};
const score=(v,key)=>Math.max(...v.batches.map(b=>b.answers[key]?.noul??0));
const eventsOf=v=>v.units.map(u=>({...u,values:Object.fromEntries(Object.keys(D.events).map(k=>[k,score(v,u.id+'_'+k)]))}));
let filter={kind:'all',key:'all'};
const transcripts=D.results.filter(v=>v.source==='transcript').length;
document.getElementById('notice').textContent='試作・未レビュー ｜ '+D.results.length+'本中、字幕あり '+transcripts+'本／タイトル・概要欄のみ '+(D.results.length-transcripts)+'本。元データは '+D.sourceFile+'。過去の控えを含み、現在の動画公開状態・活動状況は確認していません。';
const tokens=D.results.reduce((s,v)=>s+v.batches.reduce((n,b)=>n+b.usage.input_tokens,0),0);
for(const [value,label] of [[D.results.length+'本','今回の対象'],[D.results.reduce((s,v)=>s+v.units.length,0)+'区間','原文を分類'],['$'+(tokens*D.run.pricePerMillion/1e6).toFixed(5),'分類結果の入力従量費用'],[D.checks.filter(x=>x.pass).length+'/'+D.checks.length,'事前確認項目に一致']]){const n=el('div',undefined,'stat');n.append(el('b',value),el('span',label));document.getElementById('stats').append(n);}
for(const [kind,defs]of [['topic',D.topics],['project',D.projects]])for(const [key,[label]]of Object.entries(defs)){const count=D.results.filter(v=>score(v,kind+'_'+key)>=0.5).length;const high=D.results.filter(v=>score(v,kind+'_'+key)>=D.thresholds.yes).length;const b=el('button',label+' · '+count+(count>high?'（要確認 '+(count-high)+'）':''),'filter');b.type='button';b.dataset.kind=kind;b.dataset.key=key;document.getElementById(kind==='topic'?'topics':'projects').append(b);}
document.querySelectorAll('button.filter').forEach(b=>b.addEventListener('click',()=>{filter={kind:b.dataset.kind,key:b.dataset.key};document.querySelectorAll('button.filter').forEach(x=>x.classList.toggle('active',x===b));render();}));
function render(){
 const query=document.getElementById('search').value.toLowerCase(),event=document.getElementById('event').value;
 const selected=D.results.filter(v=>{if(filter.kind!=='all'&&score(v,filter.kind+'_'+filter.key)<0.5)return false;if(query&&![v.title,...v.units.map(u=>u.text)].join(' ').toLowerCase().includes(query))return false;const units=eventsOf(v);if(event==='review')return units.some(u=>Object.values(u.values).some(p=>p>D.thresholds.no&&p<D.thresholds.yes));return event==='all'||units.some(u=>u.values[event]>=D.thresholds.yes);}).sort((a,b)=>(Date.parse(a.publishedAt)-Date.parse(b.publishedAt))*(document.getElementById('order').value==='asc'?1:-1));
 document.getElementById('count').textContent=(filter.kind==='all'?'すべて':(filter.kind==='topic'?D.topics:D.projects)[filter.key][0])+' — '+selected.length+'本';
 const root=document.getElementById('timeline');root.replaceChildren();
 if(!selected.length){root.append(el('p','この条件に一致する動画はありません。','empty'));return;}
 for(const v of selected){const card=el('article',undefined,'card');card.append(el('div',v.publishedAt.slice(0,10)+' · '+(v.source==='transcript'?'時刻付き字幕':'タイトル・概要欄'),'tiny'));const h=el('h3'),a=el('a',v.title);a.href=v.url;a.target='_blank';a.rel='noopener noreferrer';h.append(a);card.append(h);const chips=el('div',undefined,'chips');for(const [key,[label]]of Object.entries(D.topics)){if(score(v,'topic_'+key)>=D.thresholds.yes)chips.append(el('span',label,'chip'));}card.append(chips);
 if(filter.kind!=='all'){const related=score(v,filter.kind+'_'+filter.key);card.append(el('p','関連候補の判定 '+related.toFixed(2)+(related<D.thresholds.yes?' · 要確認':''),'tiny'));}const units=eventsOf(v);const kinds=el('div',undefined,'chips');for(const [key,[label]]of Object.entries(D.events)){if(units.some(u=>u.values[key]>=D.thresholds.yes))kinds.append(el('span',label,'chip '+key));}card.append(kinds);
 const details=el('details');details.append(el('summary','原文と判定を確認（'+units.length+'区間）'));
 for(const u of units){const evidence=el('div',undefined,'evidence');let label=u.source==='title'?'タイトル':u.source==='description'?'概要欄':'字幕 '+Math.floor(u.start/60)+':'+String(Math.floor(u.start%60)).padStart(2,'0');evidence.append(el('div',label+' / '+u.id,'tiny'));const tags=el('div',undefined,'chips');for(const [key,[name]]of Object.entries(D.events)){const p=u.values[key];tags.append(el('span',name+' '+p.toFixed(2)+(p>D.thresholds.no&&p<D.thresholds.yes?'（要確認）':''),'chip '+(p>=D.thresholds.yes?key:'review')));}evidence.append(tags,el('div',u.text,'quote'));if(u.source==='transcript'){const jump=el('a','この時刻をYouTubeで開く');jump.href=v.url+'&t='+Math.floor(u.start)+'s';jump.target='_blank';jump.rel='noopener noreferrer';evidence.append(jump);}details.append(evidence);}
 const labels=el('details');labels.append(el('summary','トピック・プロジェクト候補の全判定値'));for(const [kind,defs]of [['topic',D.topics],['project',D.projects]])for(const [key,[name]]of Object.entries(defs)){const p=score(v,kind+'_'+key);labels.append(el('p',name+' '+p.toFixed(2)+(p>D.thresholds.no&&p<D.thresholds.yes?'（要確認）':''),'tiny'));}card.append(details,labels);root.append(card);}
}
for(const id of ['search','event','order'])document.getElementById(id).addEventListener(id==='search'?'input':'change',render);render();
const metrics=document.getElementById('metrics');metrics.append(el('p','モデル '+D.model+' ／ 今回のAPI呼び出し '+D.run.calls+'回、キャッシュ利用 '+D.run.cacheHits+'回。今回の確認済み入力 '+D.run.inputTokens.toLocaleString()+'トークン、入力従量料金換算 $'+D.run.estimatedUsd.toFixed(6)+'。'));
metrics.append(el('p','課金単価は入力100万トークン $0.042。初回接続テスト・字幕取得・他モデルは含みません。選択範囲での記述候補を示すもので、続報の有無や未完了の判定はまだ行っていません。','tiny'));
if(D.comparison){const c=D.comparison;metrics.append(el('p','一括処理の比較：6つのトピック質問を個別送信すると '+c.separateInputTokens+'トークン、一括送信では '+c.batchInputTokens+'トークン。入力 '+(c.savingFraction*100).toFixed(1)+'%削減（同じ1タイトルでの実測）。'));const ds=el('details');ds.append(el('summary','一括／個別の判定値'));for(const a of c.answers)ds.append(el('p',a.key+'：一括 '+a.batch+' ／ 個別 '+a.separate,'tiny'));metrics.append(ds);}
const table=el('table'),head=el('tr');for(const s of ['確認内容','判定値','結果'])head.append(el('th',s));table.append(head);for(const c of D.checks){const tr=el('tr');tr.append(el('td',c.reason),el('td',c.noul.toFixed(2)),el('td',c.pass?'一致':c.decision==='review'?'要確認':'不一致'));table.append(tr);}document.getElementById('checks').append(table);
document.getElementById('footer').textContent='生成 '+D.generatedAt+' / '+D.selection+' / 判定値はモデルの推定であり、正答率ではありません。';
</script></html>`;
}
module.exports={renderReport};
