# 知識マップのレビュー記録（2026-09-22）

## API・プレイリスト中心への更新（同日）

IP制限のある字幕取得を前提とせず、YouTube Data APIのuploadsと公開プレイリストを全件照合した。公開動画554本、公開プレイリスト6件のうち、このチャンネルの公開動画が含まれる5件を画面に掲載。以前の460本は一覧の取りこぼしがあったため、以下の数値は旧レビュー時点とは比較母数が異なる。

「自作MEMSマイクへの道」は公式リスト216本にMEMS以外の周辺活動も含まれるため、本編50本、同リスト内の前史3本、後続の録音検証1本を別入口の54本に整理。タイトルに基づく10の節目を表示するが、各回の題名から分からない成果は補っていない。オリジナル曲は「曲集」6本と「曲」45本を別入口にし、Producer.AI実験室5本も別にした。重複は動画ID単位で扱う。

Jevの再解析を1回100リクエスト上限・2回目150リクエスト上限（各回予算上限あり）で実施し、解析済み216本、字幕取得22本、字幕試行56本となった。字幕を取得できない動画は公式APIのタイトル・概要欄だけを根拠にしており、字幕内容や現在の完成状態を推測しない。再解析の予約額は2回合計約0.069 USDで、実請求額ではない。

Actionsは毎日から毎週月曜に変更。公開一覧とリスト所属は毎週確認し、動画詳細は新規・直近50本に絞る。成功時の公開JSONはActionsキャッシュで次回に渡し、古い動画のメタデータは手動の `full_refresh` で全件照合する。Jevと字幕は週次処理では呼ばない。

以下は更新前のレビュー記録として残す。

ローカルの `project-memory.html` を開き、検索、関連図、年表、根拠へのリンクを確認した。公開用データは460本で、字幕を取得した動画は20本、Jev解析済みは180本、公開日がある動画は338本。字幕取得の試行は49本で、そのうち29本は取得できていないかエラーになった。各数字は異なる母数であり、字幕のない動画に発言がないことは意味しない。

## 今回直した表示

- 関連図の動画名が8文字で切れていたため、日付と全タイトルを読めるリンクに変更した。
- 字幕・概要欄の検索一致箇所を動画カードに表示し、字幕ではその付近の再生時刻へ移動できるようにした。
- 公開日未取得の動画を計画・完成の集計に含めた。新しい順でも日付未取得を末尾に置く。
- 計画のリンク先が最初の40本より後にある場合も、カードを表示して移動する。
- 検索結果がないときの案内と、検索範囲の説明を追加した。

## 字幕の未取得

次の5本を優先して追加取得を試したが、`youtube-transcript-api` はアクセス制限を返した。先頭の `hdLrSdQvCmM` で別途試した `yt-dlp` も動画ページの再読み込みエラーを返し、公開ページのプレーヤーは「字幕を利用できません」と表示した。ほかの4本に字幕が存在しないと確認したわけではない。

| 動画ID | 内容 |
| --- | --- |
| `hdLrSdQvCmM` | DCプリアンプを使った新型マイクの製作 |
| `hNZ_YDGkkMw` | DC化プリアンプの製作状況 |
| `N0-nCSUjats` | REWでマイクプリアンプの周波数特性を測定 |
| `QaIRJDxbrXQ` | マイクプリアンプ用の小信号の歪率測定 |
| `nCqANcB4WQg` | NF 3625とUAC-232の入力換算ノイズ測定 |

字幕ファイルを入手できたら、`node scripts/importJevSubtitles.js --input /path/to/subtitles.srt --video 動画ID` で取り込み、`node scripts/buildProjectMemory.js --build-only` で検索データを更新する。出来事の判定も字幕で更新するには `TYPESAFE_API_KEY` を設定して `buildProjectMemory.js` を通常実行する。取得に成功するまで字幕の内容は補っていない。

公開日未取得の122本は古い控えに存在しない動画で、一覧取得だけでは日付を得られなかった。全件の現行詳細は `YOUTUBE_API_KEY` を使う `enrichMemoryCatalog.js` で更新できる。
